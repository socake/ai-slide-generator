# aippt — CLI 生成器依赖

# ── 核心 / 数据模型
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# ── 生成内核
python-pptx>=0.6.23        # PPTX 渲染
Pillow>=10.0               # 图像处理 / 素材
tenacity>=8.2              # LLM 调用重试
jsonschema>=4.21           # spec 校验兜底

# ── CLI 适配器
typer>=0.12

# ── LLM provider(按需启用,成本对比用;不配 key/不加 --provider 时不联网)
openai>=1.30               # OpenAI / OpenAI 兼容端点(DeepSeek、Qwen)
anthropic>=0.30            # Claude

# ── 预览(pptx -> png)由 LibreOffice headless 完成,系统级依赖,不在此

# ── 开发 / 测试
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
